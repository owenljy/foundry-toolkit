# Standards Reference Index

> Routing table for ServiceNow architecture standards docs. Load the doc for each component type you are designing or decomposing — not all of them, only what the current PoC touches.
>
> All paths are relative to `plugins/sn-poc/docs/`.

## Architecture

| Component type | Doc |
|----------------|-----|
| New tables, fields, relationships, indexes, choice fields | `architecture/table-design.md` |
| Roles, ACLs, field-level security, role containment | `architecture/acl-patterns.md` |
| Scoped app structure, file layout, naming conventions | `architecture/scoped-app-conventions.md` |
| REST integrations, outbound connections, MID server | `architecture/integration-patterns.md` |
